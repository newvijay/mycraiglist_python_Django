#This is a CraigList App in the area (Terre Haute,IN)
# This code is deployed to heroku and can be accessible at  https://safe-oasis-05027.herokuapp.com/
# To import the code, add 'myapp' under INSTALLED_APPS in settings.py file
#MyCraigList
=====

MyCraigList is a Django app to conduct search my mimicking the functionality of actual craiglist app but with a better
UI display.

Quick start
-----------

1. Add "myapp" to your INSTALLED_APPS setting like this::

    INSTALLED_APPS = [
        ...
        'myapp',
    ]

2. Run `python manage.py migrate` to create the polls models.

4. Start the development server and visit http://127.0.0.1:8000/
   to search a keyword.

5. Visit https://safe-oasis-05027.herokuapp.com/ to view on internet.
